function func(x) {
    var l = x * 40;
    return x + l * x;
}
























var ex2 = x => {
    var k = 56 / x;
    return x + k;
}

























var ex3 = x => {
    return x * 2;
}
















var twoparams = (x, y) => Math.sqrt(x * x + y * y)